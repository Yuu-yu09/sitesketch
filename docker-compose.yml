services:
  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_DB: sitesketch
      POSTGRES_USER: sitesketch
      POSTGRES_PASSWORD: sitesketch
    ports:
      - "5432:5432"
    volumes:
      - sitesketch-postgres:/var/lib/postgresql/data

volumes:
  sitesketch-postgres:
